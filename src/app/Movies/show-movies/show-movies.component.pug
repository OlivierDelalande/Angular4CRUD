h3 movies

div
  input([(ngModel)]="searchValue" )
  p(*ngFor="let allmovie of (allmovies | async) | showFilteredMovies:searchValue")
    a([routerLink]="['/movies/details/', allmovie.$key]") {{ allmovie.title }}


