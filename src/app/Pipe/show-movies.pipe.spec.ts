import { ShowMoviesPipe } from './show-movies.pipe';

describe('ShowMoviesPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowMoviesPipe();
    expect(pipe).toBeTruthy();
  });
});
